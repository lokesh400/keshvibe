<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Address</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 p-6 flex items-center justify-center min-h-screen">

    <div class="max-w-lg w-full bg-white p-6 rounded-lg shadow-md">
        <h2 class="text-2xl font-semibold text-gray-800 mb-4"><%= isEdit ? "Edit Address" : "Add Address" %></h2>

        <form action="<%= isEdit ? '/edit-address' : '/add-address' %>" method="POST">
            <label class="block text-gray-700 font-medium">Street:</label>
            <input type="text" name="street" value="<%= currUser.address ? currUser.address.street : '' %>"
                class="w-full p-2 border border-gray-300 rounded-lg mb-2" required>

            <label class="block text-gray-700 font-medium">City:</label>
            <input type="text" name="city" value="<%= currUser.address ? currUser.address.city : '' %>"
                class="w-full p-2 border border-gray-300 rounded-lg mb-2" required>

            <label class="block text-gray-700 font-medium">State:</label>
            <input type="text" name="state" value="<%= currUser.address ? currUser.address.state : '' %>"
                class="w-full p-2 border border-gray-300 rounded-lg mb-2" required>

            <label class="block text-gray-700 font-medium">Pincode:</label>
            <input type="text" name="pincode" value="<%= currUser.address ? currUser.address.pincode : '' %>"
                class="w-full p-2 border border-gray-300 rounded-lg mb-2" required>

            <label class="block text-gray-700 font-medium">Mobile:</label>
            <input type="text" name="mobile" value="<%= currUser.mobile ? currUser.mobile : '' %>"
                class="w-full p-2 border border-gray-300 rounded-lg mb-4" required>

            <button type="submit"
                class="w-full bg-green-500 text-white py-2 rounded-lg hover:bg-green-600">
                Save Address
            </button>
        </form>
    </div>

</body>
</html>
