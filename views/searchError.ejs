<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link rel="icon" type="image/png" href="/images/logo.png">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search Error</title>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
    <style>
        body, html {
            margin: 0;
            height: 100%;
            overflow: hidden;
            background: #111;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-direction: column;
            color: white;
            font-family: Arial, sans-serif;
            perspective: 1000px;
        }
        .error-text {
            font-size: 3rem;
            font-weight: bold;
            text-shadow: 0 0 10px rgba(255, 0, 0, 0.8);
            transform: rotateX(20deg) rotateY(-10deg);
            opacity: 0;
        }
        .btn {
            margin-top: 20px;
            background: red;
            color: white;
            padding: 10px 20px;
            border: none;
            font-size: 18px;
            cursor: pointer;
            transition: 0.3s;
            border-radius: 5px;
            opacity: 0;
        }
        .btn:hover {
            background: darkred;
        }
    </style>
</head>
<body>

    <div class="error-text">Oops! Empty Search Query</div>
    <button class="btn" onclick="window.history.back()">Go Back</button>

    <script>
        // GSAP Animations
        gsap.to(".error-text", { 
            opacity: 1, 
            duration: 1, 
            y: -20, 
            ease: "bounce.out" 
        });

        gsap.to(".error-text", {
            rotationX: 360,
            rotationY: 360,
            duration: 2,
            repeat: -1,
            ease: "linear"
        });

        gsap.to(".btn", { 
            opacity: 1, 
            duration: 1, 
            delay: 1, 
            y: 20, 
            ease: "power2.out" 
        });
    </script>

</body>
</html>
